# Environment files with real credentials — copy .env.example to .env and fill in values
nextcloud/.env
collabora/.env
nextcloud-signal/.env
.env

# Bind-mount data directories created at runtime
nextcloud/app/
nextcloud/db/
app/
db/

# macOS metadata
.DS_Store
._*
